The project can be built with strato-builder continer.

To build the container run the 'create.sh'

To test the container run 'startbuilder.sh' from this folder. Next 'cd dev/examples'. Finally 'go run hello-world.go'

To build the project from the root of the project enter the container with 'tools-docker-builder/startbuilder.sh'.
